---
/**
 * Comparison.astro — feature table vs Liveblocks / Yjs / Jazz.tools.
 *
 * Focused on true differentiators. ZeroSync column gets accent styling.
 */

type Mark = 'yes' | 'no' | 'partial'

interface Row {
  feature:    string
  zerosync:   { mark: Mark; note?: string }
  liveblocks: { mark: Mark; note?: string }
  yjs:        { mark: Mark; note?: string }
  jazz:       { mark: Mark; note?: string }
}

const rows: Row[] = [
  {
    feature:    'End-to-end encrypted',
    zerosync:   { mark: 'yes', note: 'AES-256-GCM' },
    liveblocks: { mark: 'no' },
    yjs:        { mark: 'no' },
    jazz:       { mark: 'partial', note: 'opt-in' },
  },
  {
    feature:    'Zero-knowledge server',
    zerosync:   { mark: 'yes' },
    liveblocks: { mark: 'no' },
    yjs:        { mark: 'no' },
    jazz:       { mark: 'no' },
  },
  {
    feature:    'Self-hosted',
    zerosync:   { mark: 'yes' },
    liveblocks: { mark: 'no', note: 'cloud only' },
    yjs:        { mark: 'yes' },
    jazz:       { mark: 'yes' },
  },
  {
    feature:    'Open-source',
    zerosync:   { mark: 'yes', note: 'MIT + Apache 2.0' },
    liveblocks: { mark: 'no' },
    yjs:        { mark: 'yes', note: 'MIT' },
    jazz:       { mark: 'yes', note: 'MPL-2.0' },
  },
  {
    feature:    'React hooks',
    zerosync:   { mark: 'yes' },
    liveblocks: { mark: 'yes' },
    yjs:        { mark: 'partial', note: 'community' },
    jazz:       { mark: 'yes' },
  },
]

const markColor = (m: Mark, isZerosyncCol: boolean): string => {
  if (isZerosyncCol && m === 'yes') return 'text-accent'
  if (m === 'yes')     return 'text-neutral-200'
  if (m === 'no')      return 'text-neutral-700'
  return 'text-neutral-400'  // partial
}

const markLabel = (m: Mark): string =>
  m === 'yes' ? 'Yes' : m === 'no' ? 'No' : 'Partial'
---

<section class="px-6 py-24 border-t border-neutral-900" id="comparison">
  <div class="max-w-5xl mx-auto">
    <h2 class="text-3xl sm:text-4xl font-bold text-neutral-100 mb-4">
      How it compares
    </h2>
    <p class="text-neutral-400 mb-10 max-w-2xl">
      Versus the most common alternatives for real-time collaboration.
    </p>

    <div class="overflow-x-auto -mx-6 sm:mx-0">
      <div class="min-w-[680px] px-6 sm:px-0">
        <table class="w-full border-collapse text-sm">
          <thead>
            <tr>
              <th class="text-left font-medium text-neutral-500 py-4 pr-4 w-1/4 border-b border-neutral-800"></th>
              <th class="text-left font-bold text-accent py-4 px-4 w-1/5 border-b-2 border-accent bg-accent/5 rounded-t-md">
                ZeroSync
              </th>
              <th class="text-left font-medium text-neutral-300 py-4 px-4 w-1/5 border-b border-neutral-800">Liveblocks</th>
              <th class="text-left font-medium text-neutral-300 py-4 px-4 w-1/5 border-b border-neutral-800">Yjs + y-websocket</th>
              <th class="text-left font-medium text-neutral-300 py-4 px-4 w-1/5 border-b border-neutral-800">Jazz.tools</th>
            </tr>
          </thead>
          <tbody>
            {rows.map((row) => (
              <tr class="border-b border-neutral-900 hover:bg-neutral-900/30">
                <td class="py-4 pr-4 font-medium text-neutral-200">{row.feature}</td>

                <td class="py-4 px-4 bg-accent/5">
                  <div class={markColor(row.zerosync.mark, true)}>
                    <svg aria-label={markLabel(row.zerosync.mark)} role="img" class="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      {row.zerosync.mark === 'yes' && <polyline points="20 6 9 17 4 12" />}
                      {row.zerosync.mark === 'no' && <><line x1="18" y1="6" x2="6" y2="18" /><line x1="6" y1="6" x2="18" y2="18" /></>}
                      {row.zerosync.mark === 'partial' && <line x1="5" y1="12" x2="19" y2="12" />}
                    </svg>
                  </div>
                  {row.zerosync.note && <div class="text-xs text-neutral-500 mt-1">{row.zerosync.note}</div>}
                </td>

                <td class="py-4 px-4">
                  <div class={markColor(row.liveblocks.mark, false)}>
                    <svg aria-label={markLabel(row.liveblocks.mark)} role="img" class="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      {row.liveblocks.mark === 'yes' && <polyline points="20 6 9 17 4 12" />}
                      {row.liveblocks.mark === 'no' && <><line x1="18" y1="6" x2="6" y2="18" /><line x1="6" y1="6" x2="18" y2="18" /></>}
                      {row.liveblocks.mark === 'partial' && <line x1="5" y1="12" x2="19" y2="12" />}
                    </svg>
                  </div>
                  {row.liveblocks.note && <div class="text-xs text-neutral-500 mt-1">{row.liveblocks.note}</div>}
                </td>

                <td class="py-4 px-4">
                  <div class={markColor(row.yjs.mark, false)}>
                    <svg aria-label={markLabel(row.yjs.mark)} role="img" class="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      {row.yjs.mark === 'yes' && <polyline points="20 6 9 17 4 12" />}
                      {row.yjs.mark === 'no' && <><line x1="18" y1="6" x2="6" y2="18" /><line x1="6" y1="6" x2="18" y2="18" /></>}
                      {row.yjs.mark === 'partial' && <line x1="5" y1="12" x2="19" y2="12" />}
                    </svg>
                  </div>
                  {row.yjs.note && <div class="text-xs text-neutral-500 mt-1">{row.yjs.note}</div>}
                </td>

                <td class="py-4 px-4">
                  <div class={markColor(row.jazz.mark, false)}>
                    <svg aria-label={markLabel(row.jazz.mark)} role="img" class="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                      {row.jazz.mark === 'yes' && <polyline points="20 6 9 17 4 12" />}
                      {row.jazz.mark === 'no' && <><line x1="18" y1="6" x2="6" y2="18" /><line x1="6" y1="6" x2="18" y2="18" /></>}
                      {row.jazz.mark === 'partial' && <line x1="5" y1="12" x2="19" y2="12" />}
                    </svg>
                  </div>
                  {row.jazz.note && <div class="text-xs text-neutral-500 mt-1">{row.jazz.note}</div>}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>
